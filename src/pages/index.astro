<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Death Ponee</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Boogaloo&display=swap" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Special+Elite&display=swap" rel="stylesheet" />

  <style>
    body {
      margin: 0;
      font-family: 'Special Elite', monospace;
      background-color: #000;
      color: #f2f2f2;
      text-align: center;
      padding: 1rem 1rem 3rem 1rem;
    }

    h1 {
      font-family: 'Boogaloo', sans-serif;
      font-size: 5rem;
      text-transform: uppercase;
      letter-spacing: 3px;
      color: #ffcf00;
      margin: 0 0 1rem;
      transform: rotate(-1deg);
    }

    h2 {
      font-family: 'Boogaloo', sans-serif;
      font-size: 1.5rem;
      color: #ffcf00;
      text-transform: uppercase;
      margin-bottom: 2rem;
    }

    .subheading {
      font-family: 'Special Elite', monospace;
      font-size: 0.9rem;
      color: #f2f2f2;
      margin-bottom: 2rem;
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    .album-covers {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 2rem;
      margin-bottom: 3rem;
    }

    .album-covers img {
      max-width: 300px;
      height: auto;
      border-radius: 0;
      box-shadow: none;
      filter: contrast(110%) grayscale(30%);
      animation: jitter 0.4s infinite;
    }

    @keyframes jitter {
      0% { transform: rotate(0deg); }
      50% { transform: rotate(1deg); }
      100% { transform: rotate(-1deg); }
    }

    .links {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
      margin-bottom: 4rem;
    }

    .links a {
      font-family: 'Special Elite', monospace;
      background: transparent;
      color: #ff0033;
      border: 2px dashed #ff0033;
      text-decoration: none;
      padding: 0.6rem 1rem;
      text-transform: uppercase;
      transition: none;
      font-size: 0.9rem;
    }

    .links a:hover {
      background-color: #ff0033;
      color: #000;
    }

    #about {
      padding: 3rem 1rem;
    }

    .band-photo img {
      width: 600px;
      max-width: 90%;
      height: auto;
      border-radius: 0;
      box-shadow: none;
      filter: contrast(110%) grayscale(20%);
      opacity: 1;
    }

    #typewriter {
      max-width: 700px;
      margin: 2rem auto;
      font-size: 1rem;
      line-height: 1.7;
      text-align: justify;
      color: #d6d6d6;
    }

    #typewriter::after {
      content: '|';
      animation: blink 0.8s steps(1) infinite;
    }

    @keyframes blink {
      50% {
        opacity: 0;
      }
    }

    #typewriter p.yellow {
      color: #ffcf00 !important;
      font-weight: bold !important;
    }

    #who {
      padding: 3rem 1rem;
    }

    .who-flex {
      display: flex;
      justify-content: center;
      align-items: flex-start;
      gap: 2rem;
      max-width: 1000px;
      margin: 0 auto;
      flex-wrap: wrap;
    }

    .band-selfie img {
      width: 300px;
      max-width: 100%;
      height: auto;
      border-radius: 0;
      box-shadow: none;
      filter: contrast(110%) grayscale(30%);
    }

    .band-members {
      max-width: 700px;
      margin: 1rem auto;
      font-size: 1rem;
      line-height: 1.7;
      text-align: justify;
      color: #d6d6d6;
    }

    .band-members .member {
      margin-bottom: 1.5rem;
    }

    .name {
      color: #ff0033;
      font-weight: bold;
    }

    @media (max-width: 700px) {
      h1 {
        font-size: 3rem;
        letter-spacing: 1px;
      }

      .album-covers img {
        max-width: 45%;
      }

      .links a {
        font-size: 0.8rem;
        padding: 0.5rem 0.8rem;
      }
    }
  </style>
</head>

<body>
  <section id="home">
    <h1>Death Ponee</h1>
    <div class="subheading">New album: <em>Waiting for Rover</em> – out now!</div>

    <div class="album-covers">
      <img src="/Final Front copy_for Digital (2).jpg" alt="Album cover front" />
      <img src="/Final Back copy_for Digital.jpg" alt="Album cover back" />
    </div>

    <div class="links">
      <a href="https://open.spotify.com/album/7DLH0CwDNEX6SX8OLLb3H5" target="_blank">Spotify</a>
      <a href="https://music.apple.com/us/album/waiting-for-rover-ep/1822589894" target="_blank">Apple Music</a>
      <a href="https://www.youtube.com/playlist?list=OLAK5uy_lqEcVBijeakOThABs2QHeLU7wPVUiZ3EU" target="_blank">YouTube</a>
      <a href="https://music.amazon.com/albums/B0FF6TZ33Q" target="_blank">Amazon</a>
    </div>
  </section>

  <section id="about">
    <h2>What are we?</h2>
    <div class="band-photo">
      <img src="/Band photo2.png" alt="Death Ponee band photo" />
    </div>
    <div id="typewriter"></div>
  </section>

  <section id="who">
    <h2>Who are we?</h2>

    <div class="who-flex">
      <div class="band-selfie">
        <img src="/Bandselfie.jpg" alt="Band selfie" />
      </div>

      <div class="band-members">
        <div class="member">
          <span class="name">Matt Ford</span> – Synths, vocals, and occasional existential dread. Former cellist, now armed with circuits and sarcasm.
        </div>
        <div class="member">
          <span class="name">Mark Tierney</span> – Guitarist. Never seems to age — possibly immortal. Thinks fuzz pedals are a food group.
        </div>
        <div class="member">
          <span class="name">George Allen</span> – Synths and vocals. Meticulous, melodic, and almost certainly has a colour-coded band spreadsheet.
        </div>
        <div class="member">
          <span class="name">Nick Jennings</span> – Drums. Solid as a rock. Possibly the banjo king of Prague — but we try not to encourage him.
        </div>
      </div>
    </div>
  </section>

  <script>
    const paragraphs = [
      {
        text: "Death Ponee is an electronic post-punk band from Prague. Gritty guitars, warped synths, and live drums — no safety net.",
        class: "yellow"
      },
      {
        text: "It started with Matt Ford, a lapsed cellist messing around on a digital piano. The songs were cold and lo-fi. He called up some old collaborators, grabbed a synth, and it spiralled from there.",
        class: ""
      },
      {
        text: "The first record, Life’s Great Uniform, was mostless wordless (and harmless). Just textures and tension. On Waiting for Rover, Ford finally opens his mouth. Now it’s a four-piece: two synths, guitar, drums — plus a glitchy 8-bit sampler called the MicroGranny spitting nonsense at the edge of it all.",
        class: ""
      }
    ];

    const container = document.getElementById("typewriter");
    let pIndex = 0;
    let charIndex = 0;
    let currentP;

    function typeWriter() {
      if (pIndex >= paragraphs.length) return;

      if (charIndex === 0) {
        currentP = document.createElement("p");
        if (paragraphs[pIndex].class) {
          currentP.classList.add(paragraphs[pIndex].class);
        }
        container.appendChild(currentP);
      }

      currentP.innerHTML += paragraphs[pIndex].text.charAt(charIndex);
      charIndex++;

      if (charIndex < paragraphs[pIndex].text.length) {
        setTimeout(typeWriter, 30);
      } else {
        charIndex = 0;
        pIndex++;
        setTimeout(typeWriter, 500);
      }
    }

    typeWriter();
  </script>
</body>
</html>
